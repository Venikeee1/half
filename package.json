{
  "name": "half-year",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:copy": "vite build && cp -a dist/. /var/www/html"
  },
  "devDependencies": {
    "vite": "^4.1.0"
  },
  "dependencies": {
    "normalize.css": "^8.0.1",
    "swiper": "^9.0.4"
  }
}
